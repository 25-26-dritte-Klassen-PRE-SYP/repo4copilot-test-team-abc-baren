{
  "name": "casino-testsystem",
  "private": true,
  "version": "1.0.0",
  "description": "Root scripts for frontend and backend",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev:all": "concurrently \"node ./scripts/run-with-env.js backend npm run dev\" \"node ./scripts/run-with-env.js frontend npm run dev\"",

    "dev:backend": "node ./scripts/run-with-env.js backend npm run dev",
    "dev:frontend": "node ./scripts/run-with-env.js frontend npm run dev",

    "build:frontend": "npm run build --prefix frontend",
    "start:backend": "npm run start --prefix backend",
    "vercel-build": "npm run build:frontend"
  },
  "dependencies": {
    "serverless-http": "^3.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
